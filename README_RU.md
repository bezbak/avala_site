# Avala — Корпоративный веб-сайт

Современный многостраничный веб-сайт для консалтинговой компании Avala, построенный на React, TypeScript и Tailwind CSS.

## 🚀 Быстрый старт

### Требования
- Node.js 18+ и npm

### Локальный запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

Приложение будет доступно по адресу `http://localhost:8080`

## 📦 Деплой на Vercel (рекомендуется)

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Vercel автоматически определит настройки сборки
4. Нажмите "Deploy"

Или используйте Vercel CLI:

```bash
npm i -g vercel
vercel
```

## 🎨 Структура проекта

```
src/
├── components/         # Переиспользуемые компоненты
│   ├── ui/            # UI компоненты (shadcn)
│   ├── Header.tsx     # Навигационная шапка
│   ├── Footer.tsx     # Подвал сайта
│   ├── FloatingContact.tsx  # Плавающая кнопка контактов
│   └── SEO.tsx        # SEO компонент
├── pages/             # Страницы приложения
│   ├── Home.tsx       # Главная страница
│   ├── Services.tsx   # Страница услуг
│   ├── Cases.tsx      # Кейсы клиентов
│   ├── About.tsx      # О компании
│   ├── Team.tsx       # Команда
│   └── Contacts.tsx   # Контакты и форма
└── App.tsx            # Роутинг приложения
```

## ✏️ Редактирование контента

### Изменение текста на страницах

Весь контент находится в файлах страниц в папке `src/pages/`. Просто откройте нужную страницу и отредактируйте текст.

**Пример** (src/pages/Home.tsx):
```tsx
<h1>Помогаем бизнесу расти и развиваться</h1>
<p>Ваш новый текст здесь</p>
```

### Изменение услуг (src/pages/Services.tsx)

Найдите массив `services` и отредактируйте:
```tsx
const services = [
  {
    icon: BarChart3,
    title: "Название услуги",
    problem: "Описание проблемы",
    solution: "Ваше решение",
    benefits: ["Выгода 1", "Выгода 2", ...]
  },
  // ...
];
```

### Изменение кейсов (src/pages/Cases.tsx)

Отредактируйте массив `cases`:
```tsx
const cases = [
  {
    client: "Название клиента",
    industry: "Отрасль",
    before: { /* метрики ДО */ },
    after: { /* метрики ПОСЛЕ */ },
    solution: ["Шаг 1", "Шаг 2", ...],
    // ...
  }
];
```

### Изменение команды (src/pages/Team.tsx)

Отредактируйте массив `team`:
```tsx
const team = [
  {
    name: "Имя Фамилия",
    role: "Должность",
    expertise: ["Навык 1", "Навык 2"],
    bio: "Краткая биография",
    image: "URL изображения",
    // ...
  }
];
```

### Изменение контактной информации

1. **Header/Footer** (src/components/Header.tsx, Footer.tsx): Измените телефоны, email, адрес
2. **Contacts page** (src/pages/Contacts.tsx): Обновите массив `contactInfo`
3. **Floating button** (src/components/FloatingContact.tsx): Обновите ссылки на WhatsApp и телефон

### Изменение цветов и стилей

Все цвета определены в дизайн-системе в файле `src/index.css`:

```css
:root {
  --primary: 38 65% 68%;  /* Основной цвет (золотой) */
  --foreground: 0 0% 0%;  /* Цвет текста (черный) */
  /* ... другие переменные */
}
```

## 🎯 SEO и метатеги

Каждая страница использует компонент `<SEO>` для управления метатегами:

```tsx
<SEO
  title="Заголовок страницы"
  description="Описание для поисковых систем"
  keywords="ключевые, слова"
/>
```

**Sitemap** находится в `public/sitemap.xml` — обновите домен перед деплоем.

## 🔧 Технологии

- **React 18** — UI библиотека
- **TypeScript** — типизация
- **Vite** — сборщик и dev-сервер
- **Tailwind CSS** — стилизация
- **React Router** — маршрутизация
- **React Hook Form + Zod** — валидация форм
- **shadcn/ui** — UI компоненты
- **Lucide React** — иконки

## 📱 Адаптивность

Сайт полностью адаптивен и протестирован на:
- Мобильных устройствах (320px+)
- Планшетах (768px+)
- Десктопах (1024px+, 1440px+)

## ♿ Доступность

- Семантический HTML5
- ARIA-атрибуты где необходимо
- Контрастность цветов соответствует WCAG
- Клавиатурная навигация

## 📊 Производительность

- Lazy loading изображений
- Оптимизированные шрифты (Google Fonts с preconnect)
- Минификация и tree-shaking в production
- Быстрый Vite dev-сервер

## 🌐 Интернационализация

Сейчас сайт на русском языке. Для добавления других языков:
1. Установите `react-i18next`
2. Создайте файлы переводов
3. Оберните приложение в `I18nextProvider`

## 📝 Структурированные данные (JSON-LD)

Для улучшения SEO можно добавить JSON-LD схемы в `index.html`:

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "Avala",
  "url": "https://yourdomain.com",
  "logo": "https://yourdomain.com/logo.png",
  "contactPoint": {
    "@type": "ContactPoint",
    "telephone": "+7-495-123-45-67",
    "contactType": "Customer Service"
  }
}
</script>
```

## 🐛 Отладка

```bash
# Просмотр логов сборки
npm run build

# Анализ bundle size
npm run build -- --mode analyze
```

## 📄 Лицензия

Proprietary — все права принадлежат компании Avala

## 🤝 Поддержка

Для вопросов и поддержки: tech@avala.ru

---

**Важное примечание**: Это Vite + React проект. Для использования Next.js с SSR необходим порт на Next.js.
